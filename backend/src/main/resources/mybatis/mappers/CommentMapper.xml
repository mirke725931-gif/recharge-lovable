<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.recharge.backend.comment.dao.CommentDAO">

    <!-- ✅ 댓글 목록 조회 -->
    <select id="selectComments"
            parameterType="map"
            resultType="com.recharge.backend.comment.vo.CommentVO">
        SELECT
        COMMENT_ID,
        COMMENT_TARGET_TYPE,
        COMMENT_TARGET_ID,
        USER_ID,
        COMMENT_CONTENT,
        CREATE_DATE,
        CREATE_ID,
        UPDATED_DATE,
        UPDATED_ID
        FROM TB_COMMENT
        WHERE COMMENT_TARGET_TYPE = #{targetType}
        AND COMMENT_TARGET_ID = #{targetId}
        ORDER BY CREATE_DATE ASC
    </select>

    <!-- ✅ 댓글 등록  -->
    <insert id="insertComment"
            parameterType="com.recharge.backend.comment.vo.CommentVO">

        <selectKey keyProperty="commentId" resultType="long" order="BEFORE">
            SELECT NVL(MAX(COMMENT_ID), 0) + 1 FROM TB_COMMENT
        </selectKey>

        INSERT INTO TB_COMMENT (
        COMMENT_ID,
        COMMENT_TARGET_TYPE,
        COMMENT_TARGET_ID,
        USER_ID,
        COMMENT_CONTENT,
        CREATE_DATE,
        CREATE_ID,
        UPDATED_DATE,
        UPDATED_ID
        ) VALUES (
        #{commentId},
        #{commentTargetType},
        #{commentTargetId},
        #{userId},
        #{commentContent},
        SYSDATE,
        #{userId},
        SYSDATE,
        #{userId}
        )
    </insert>

    <!-- ✅ 댓글 삭제 -->
    <delete id="deleteComment" parameterType="long">
        DELETE FROM TB_COMMENT
        WHERE COMMENT_ID = #{commentId}
    </delete>




</mapper>
